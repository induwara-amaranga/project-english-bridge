import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    // Forwards to the Spring Boot API (see server/) so the browser sees
    // everything as same-origin — the httpOnly refresh cookie then works
    // without any cross-origin cookie/CORS configuration in dev.
    proxy: {
      '/api': {
        target: process.env.VITE_API_PROXY_TARGET || 'http://localhost:8080',
        changeOrigin: true,
      },
    },
  },
})
